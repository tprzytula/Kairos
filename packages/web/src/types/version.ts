export interface VersionInfo {
  version: string
  fullVersion: string
  buildTime: string
  buildTimeUTC: string
  buildTimestamp: number
  packageVersion: string
  versionFormat: string
  cacheKey: string
}