import { styled } from "@mui/system";

export const Container = styled('div')({
    position: 'absolute',
    bottom: '5em',
    left: 0,
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
})
